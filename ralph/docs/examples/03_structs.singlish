kampung main

// Define a struct
pattern Person susun {
    Name tar
    Age  nombor
}

// Define a function
action SayHello(p ki Person) {
    gong("Hello, my name is " + p.Name)
}

action boss() {
    // Create a new Person
    var uncle Person
    
    // Set fields
    uncle.Name = "Uncle Roger"
    uncle.Age = 50

    // Call the function
    SayHello(&uncle)
}
